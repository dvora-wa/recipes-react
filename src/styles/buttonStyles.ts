const buttonStyles = {
    fontWeight: 'bold',
    transition: 'background-color 0.3s ease',
};

const cancelButtonStyles = {
    ...buttonStyles,
    color: '#ff5252', 
};

const loginButtonStyles = {
    ...buttonStyles,
    color: '#42a5f5', 
};

const registerButtonStyles = {
    ...buttonStyles,
    color: '#66bb6a', 
};

export { cancelButtonStyles, loginButtonStyles, registerButtonStyles };
